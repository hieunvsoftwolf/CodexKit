# Development Scripts

This directory stays intentionally minimal during the clean restart.

Pre-Phase 1 scope:
- docs-safe helper scripts only
- knowledge-graph maintenance helpers only
- do not add runtime bootstrap, daemon smoke, or DB cleanup scripts until a git-backed Phase 1 `BASE_SHA` exists
