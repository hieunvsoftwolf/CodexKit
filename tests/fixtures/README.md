# Test Fixtures

This directory is reserved for future executable runtime fixtures.

During the clean restart it should stay free of discarded Phase 1 ledger or SQLite artifacts.
Create runtime fixtures only after the git-backed Phase 1 wave starts from a pinned `BASE_SHA`.
