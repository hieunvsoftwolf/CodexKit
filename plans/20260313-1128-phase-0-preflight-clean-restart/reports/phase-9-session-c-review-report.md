# Phase 9 Session C Review Report

- Date: 2026-03-25
- Status: completed
- Session Role: reviewer
- Source of truth: current candidate worktree at `/Users/hieunv/Claude Agent/CodexKit`, pinned `BASE_SHA` `8a7195c2a98253dd1060f9680b422b75d139068d`, current Phase 9 docs, Session A summary, Session B0 artifact, current control-state snapshot
- Exclusions honored: no tester output, no verdict output

## Findings

### CRITICAL

- Evidence provenance is incorrect, so the Phase 9 artifacts do not identify the actual candidate under review. [tests/runtime/helpers/phase9-evidence.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/helpers/phase9-evidence.ts#L19) records `git rev-parse HEAD` as the bundle commit, but the control-state explicitly says the candidate is an uncommitted worktree beyond `BASE_SHA`. The generated bundles and release report therefore all claim commit `8a7195c2a98253dd1060f9680b422b75d139068d` instead of the real candidate state. See [control-state-phase-9-wave-2-ready-after-s2-s3.md](/Users/hieunv/Claude%20Agent/CodexKit/plans/20260313-1128-phase-0-preflight-clean-restart/reports/control-state-phase-9-wave-2-ready-after-s2-s3.md#L8), [validation-golden-evidence.json](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/validation-golden-evidence.json#L3), [validation-migration-evidence.json](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/validation-migration-evidence.json#L3), and [release-readiness-report.md](/Users/hieunv/Claude%20Agent/CodexKit/plans/20260313-1128-phase-0-preflight-clean-restart/reports/release-readiness-report.md#L3). That breaks the frozen `base_sha`/`candidate_sha` contract and makes evidence reuse or freshness judgments non-auditable.

- The shared Phase 9 validation schema drifted from the frozen acceptance and cannot represent required release evidence states. [packages/codexkit-core/src/domain-types.ts](/Users/hieunv/Claude%20Agent/CodexKit/packages/codexkit-core/src/domain-types.ts#L109) hardcodes metric status to `pass | fail | missing`, which removes the required `blocked` state. The bundle shape in [packages/codexkit-core/src/domain-types.ts](/Users/hieunv/Claude%20Agent/CodexKit/packages/codexkit-core/src/domain-types.ts#L127) omits `base_sha`, `candidate_sha`, and any `fresh_until` or explicit freshness-rule field, and metric evidence is only free-form strings instead of durable artifact references. [packages/codexkit-daemon/src/workflows/contracts.ts](/Users/hieunv/Claude%20Agent/CodexKit/packages/codexkit-daemon/src/workflows/contracts.ts#L247) validates only that those strings are non-empty, so the contract test cannot catch the missing provenance/freshness/reporting fields that Session B0 froze as mandatory.

- The release-readiness synthesis violates the required report contract and marks metrics using reused evidence without verifying applicability to the candidate. [tests/runtime/runtime-workflow-phase9-release-readiness.integration.test.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/runtime-workflow-phase9-release-readiness.integration.test.ts#L47) imports `.tmp/phase-5-wave2-nfr-evidence.json` and immediately converts its results into Phase 9 pass rows, but that file was generated for commit `9f2cfce33796cc96fb92ad64f4194c0e852e18f0`, not the current candidate ([phase-5-wave2-nfr-evidence.json](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/phase-5-wave2-nfr-evidence.json#L2)). The generated table in [release-readiness-report.md](/Users/hieunv/Claude%20Agent/CodexKit/plans/20260313-1128-phase-0-preflight-clean-restart/reports/release-readiness-report.md#L13) also uses `missing` instead of `blocked`, omits the required freshness disposition, omits fixture ids, omits host-manifest refs or N/A justifications, and lists suite names like `validation-chaos` instead of durable artifact refs. That is contract drift and report-surface dishonesty, even though the overall verdict stays `fail`.

### IMPORTANT

- The migration checklist does not prove two of the frozen required rows. Session B0 required rows for “golden workflow suite passes on fixture repos” and “chaos scenarios recover without orphaning critical state”, but the implementation only checks whether the golden and chaos evidence files exist. See [tests/runtime/runtime-workflow-phase9-migration-checklist.integration.test.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/runtime-workflow-phase9-migration-checklist.integration.test.ts#L176) and the emitted checklist rows [migration-validation-checklist.md](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/migration-validation-checklist.md#L11). That means the checklist can report success on those rows without proving the required fixture-level behavior. The checklist artifact also encodes `yes/no` instead of the required `pass/fail/blocked` semantics ([tests/runtime/runtime-workflow-phase9-migration-checklist.integration.test.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/runtime-workflow-phase9-migration-checklist.integration.test.ts#L197)).

- Host-manifest completeness is effectively waived by implementation. [tests/runtime/helpers/runtime-fixture.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/helpers/runtime-fixture.ts#L45) always emits `filesystem: "unknown"` and defaults `codexCliVersion` to `"unknown"`, and [packages/codexkit-daemon/src/workflows/contracts.ts](/Users/hieunv/Claude%20Agent/CodexKit/packages/codexkit-daemon/src/workflows/contracts.ts#L275) treats any non-empty string as valid. The generated bundles confirm both values are unknown ([validation-golden-evidence.json](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/validation-golden-evidence.json#L91), [validation-migration-evidence.json](/Users/hieunv/Claude%20Agent/CodexKit/.tmp/validation-migration-evidence.json#L120)). Session B0 froze host-manifest completeness as mandatory, so this validator currently accepts incomplete host evidence as if it were complete.

- The chaos suite does not actually exercise the required “artifact publish before task update” seam and leaves several required invariants unproven. The `crash after publish` case in [tests/runtime/runtime-workflow-phase9-chaos-recovery.integration.test.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/runtime-workflow-phase9-chaos-recovery.integration.test.ts#L59) only writes a file into `allowed/` and exits; it never performs an artifact publication step, so it cannot prove the spec’s artifact-publish/task-update split. The metric then treats any artifact count as success at [tests/runtime/runtime-workflow-phase9-chaos-recovery.integration.test.ts](/Users/hieunv/Claude%20Agent/CodexKit/tests/runtime/runtime-workflow-phase9-chaos-recovery.integration.test.ts#L71). More broadly, the suite does not assert “no ledger corruption” or “explicit operator-visible recovery state” for each required chaos case, so even a future green run would still under-cover the frozen acceptance surface.

## Open Questions Or Assumptions

- Assumed the `.tmp/validation-*.json`, `.tmp/migration-validation-checklist.md`, and `release-readiness-report.md` artifacts are part of the candidate implementation surface because the control-state and Session A summary name them as required Phase 9 outputs.
- Review was static against code and generated artifacts only; I did not rerun tests and did not use tester or verdict artifacts.
- No other open questions.
